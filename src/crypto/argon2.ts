/**
 * Re-export of Argon2id passphrase KDF from
 * `@de-otio/crypto-envelope/primitives`. Still exposed as `argon2Derive`
 * through chaoskb's crypto index.
 */
export { deriveFromPassphrase } from '@de-otio/crypto-envelope/primitives';
